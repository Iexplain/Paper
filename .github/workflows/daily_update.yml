name: Daily Paper Update

on:
  schedule:
    - cron: '0 0 */3 * *'  # 每 3 天的 UTC 时间 0:00 运行
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 检出仓库
      - uses: actions/checkout@v3

      # 2️⃣ 设置 Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 3️⃣ 安装依赖
      - name: Install dependencies
        run: pip install -r requirements.txt

      # 4️⃣ 抓取 Semantic Scholar 文献
      - name: Fetch Semantic Scholar papers
        run: python scripts/fetch_s2.py

      # 5️⃣ 生成网页
      - name: Generate HTML
        run: python scripts/generate_web.py

      # 6️⃣ Commit & Push（使用 GITHUB_TOKEN）
      - name: Commit and push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add -f data/*.json index.html
          git commit -m "Daily paper update" || echo "No changes"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }} HEAD:main
